<table data-bind="page: {id: 'PoorCredit', with: poorcredit}" class="table table-hover">
    <thead>
        <tr class="table-row">
            <th class="col-large"></th>
            <th class="col-medium">
                <a href="#">Offer Details</a>
            </th>
            <th class="col-small" data-bind="click: App.PoorCreditController.Table.sortByApr">
                <a href="#">APR</a>
            </th>
            <th class="col-small" data-bind="click: App.PoorCreditController.Table.sortByEligibility">
                <a href="#">Eligibility</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody data-bind="foreach: cards">
        @Html.Partial(Url.Content("~/Views/_Partials/PoorCredit/_TableRow.cshtml"))
    </tbody>
</table>
